version: 2
before:
  hooks:
    - make test-go

builds:
  - id: nqcli
    main: ./main.go
    binary: nq
    env:
      - CGO_ENABLED=0
    flags:
      - -trimpath
    ldflags:
      - -s -w -X github.com/ankit-lilly/nqcli/cmd.version={{ .Version }}
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64

archives:
  - id: nqcli-archive
    formats: 
     - tar.gz
    format_overrides:
      - goos: windows
        formats: 
         - zip
    files:
      - README.md

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
